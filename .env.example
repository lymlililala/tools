# Supabase admin secret key (NEVER commit the real value)
# Get it from: Supabase Dashboard → Project Settings → API Keys → Secret keys
SUPABASE_SECRET_KEY=sb_secret_xxx

# Optional: override the project URL (defaults to the production project)
# SUPABASE_URL=https://tixgzezefjjsyuzgdhcd.supabase.co
